{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认权限（MOSE alpha 仅启用 core 默认权限，后续按需收紧）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
