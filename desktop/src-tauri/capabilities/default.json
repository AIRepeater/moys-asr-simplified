{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MOSE 主窗口默认权限；外部程序只能由 Rust 命令按需调用",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
